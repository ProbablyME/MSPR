# Config de l'API FastAPI (copier en .env, ne pas committer le vrai .env)
DB_USER=postgres
DB_PASSWORD=change-me
DB_HOST=localhost
DB_PORT=5433
DB_NAME=postgres
API_TOKEN=change-me          # OBLIGATOIRE — pas de valeur par défaut côté code
