# ═══════════════════════════════════════════════════════════════
# Dépendances ETL — Pipeline RailCarbon (etl.py)
# ═══════════════════════════════════════════════════════════════
# Installation : pip install -r requirements.txt
# Prérequis système : Java 11 ou 17 (pour PySpark)

# ── Traitement distribué ──────────────────────────────────────
pyspark>=3.5

# ── Base de données PostgreSQL ────────────────────────────────
psycopg2-binary>=2.9

# ── Lecture fichier Excel EASA ────────────────────────────────
openpyxl>=3.1

# ── Téléchargement HTTP (GTFS, OPDI) ─────────────────────────
requests>=2.31

# ── Gestion des dates (relativedelta) ─────────────────────────
python-dateutil>=2.8

# ── Correction country_code par GPS ───────────────────────────
reverse_geocode>=1.6

# ── Monitoring logs → Grafana (optionnel) ─────────────────────
python-logging-loki>=0.3
